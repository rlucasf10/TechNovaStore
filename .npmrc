# NPM Configuration for TechNovaStore
# Only show high and critical security vulnerabilities
audit-level=high

# Disable funding messages
fund=false

# Use exact versions for better reproducibility
save-exact=true