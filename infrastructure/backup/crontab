# TechNovaStore Database Backup Crontab
# Run daily backup at 2:00 AM
0 2 * * * /scripts/backup-all.sh >> /logs/backup.log 2>&1

# Run weekly full backup on Sundays at 3:00 AM
0 3 * * 0 /scripts/backup-full.sh >> /logs/backup-full.log 2>&1

# Clean old backups daily at 4:00 AM
0 4 * * * /scripts/cleanup-backups.sh >> /logs/cleanup.log 2>&1

# Health check every 5 minutes
*/5 * * * * /scripts/health-check.sh >> /logs/health.log 2>&1